import AppLayout from '../../components/Layout/AppLayout';

/**
 * ZenBoard layout wrapper.
 * Acts as the container for the multi-page authenticated app.
 */
export default function ZenBoard() {
  return <AppLayout />;
}
